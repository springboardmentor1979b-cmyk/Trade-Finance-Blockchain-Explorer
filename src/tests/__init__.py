"""Tests package.

Contains test suites for authentication endpoints, services, and utilities.
Uses pytest fixtures and mocking for test isolation.

Test Modules:
    - test_auth: Tests for authentication endpoints (register, login, refresh, logout)
    - conftest: Pytest configuration, fixtures, and dependency overrides
"""
