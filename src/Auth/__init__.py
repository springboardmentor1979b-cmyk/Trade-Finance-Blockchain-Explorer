"""Authentication package.

Contains authentication-related modules for user registration, login, token
management, and access control. Includes services, schemas, utilities, and
FastAPI routers.

Modules:
    - authService: Business logic for authentication operations
    - schemas: Pydantic models for request/response validation
    - utils: Cryptographic utilities and JWT token handling
    - router: FastAPI route handlers for authentication endpoints
    - dependency: FastAPI dependencies for token validation and user extraction
"""
